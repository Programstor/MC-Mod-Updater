name: Auto Pull MC Mod Updater

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull latest code
        run: |
          cd ~/scripts/mod-updater
          git pull origin main